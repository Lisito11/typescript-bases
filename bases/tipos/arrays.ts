(() => {


    // const numbers = [1,2,3,4,5, "6", true];
    const numbers: number[] = [1,2,3,4,5];

    // numbers.push(true)
    const villains = ['Hello', 'Hello 2'];

    villains.forEach( v => console.log(v.toUpperCase()))

    console.log(numbers);
    







})()